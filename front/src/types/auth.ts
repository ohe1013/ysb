export interface LoginUserType {
    email: string;
    password: string;
}
export interface SignUpUserType {
    email: string;
    password: string;
    username: string;
}

export interface AuthResType {
    accessToken: string;
}
